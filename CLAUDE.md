# 개발 프로젝트 작업 방식 (범용)

> 툴/플러그인/앱/서비스 등 도메인 무관 적용 가능한 사용자–Claude 협업 표준.
> 신규 프로젝트 시작 전 본 문서를 컨텍스트로 먼저 주입.
> 목표: "굴러가게 만드는 법"이 아니라 **"굴러가다 망가져도 복구가 빠르고, 학습이 누적되게"**.

---

## 0. 핵심 전제

1. **Vertical slice → 점진 확장**. 처음부터 완벽한 모듈 X.
2. **End-to-end 검증 단위로 자른다**. 각 phase 독립 동작 검증 가능해야.
3. **꼬이면 즉시 reset + 더 작은 단위로 재진입**. 같은 단위 재시도 = 같은 꼬임.
4. **사후 격리보다 사전 가시화**. Pending/Quarantine/Resolve 같은 사후 워크플로우보다 변환 *전* 다이얼로그/프리뷰가 우월.
5. **모호한 입력은 자동 추정 금지, 차단**. 자동 보정이 사용자 의도와 일치 확신 없으면 throw.
6. **메모리/Lessons 즉시 적층**. 같은 교정을 두 번 받지 않는다.

---

## 1. 프로젝트 시작

### 1.1 Vision 문서 먼저
- 목적 / 입출력 / 컨벤션 / **비범위(Out of Scope)** 한 md에 못 박는다. 비범위 명시가 더 중요.
- Vision은 합의 후 동결. 작업 중 정책 충돌은 사용자 합의로 *수정* 가능, 단 phase 문서에 변경 이력 표기.

### 1.2 Phase 맵 한번에 작성
- Phase 0(Setup) → 1(코어 1슬라이스) → ... → 마지막(패키징/배포) 단일 표.
- 각 phase **목표 / DoD / 의존** 3열.
- 분해 원칙: **인프라 → 기능 → UI/표면 → 정리** 순서.
- Renumber 자연 발생 → 우회 phase 끼면 후속 phase 번호 옮김. 처음부터 완벽한 번호 매기지 X.

### 1.3 폴더 레이아웃
```
tasks/{ProjectName}/
├── 00_PhaseOverview.md      ← 로드맵 + 진행 상태
├── NN_PhaseN_*.md           ← phase 직전 직접 작성
├── done/                    ← 완료 후 회고 추가하고 이동
└── lessons.md               ← 프로젝트별 학습 누적 (세션 시작 시 1회 읽기)
```

---

## 2. Phase 진행 패턴

### 2.1 To-do 우선
- 코드 전 phase 문서 작성. Area A/B/C로 쪼개고 각 DoD 명시.
- 검증 케이스는 to-do 단계에서 미리 나열 (사후 끼우기 = 누락).

### 2.2 Sub-phase 분할 기준
- 통합 UI / 다축 동시 변경 / 한 번 깨지면 진단 어려운 영역.
- 순서: **Headless 코어 → Minimal 표면 → 미리보기(read-only) → 옵션/토글 → 정리**.
- "헤드리스 코어가 단독 통과하지 않으면 UI는 만들지 않는다"가 황금률.

### 2.3 꼬이면 재진입
- **반드시 더 작은 vertical slice로**. 같은 단위 재시도 = 같은 꼬임.
- 재진입 후 꼬임 방지 원칙은 phase 문서 상단에 못 박음.

### 2.4 정리 sub-phase는 마지막에 명시적으로
- 신규 기능 0건. 삭제·검증·회고만.
- DoD: 콘솔/린트 에러 0건 + 회귀 시나리오 통과.
- "디버그용 미사용 데드 코드 보존"의 함정 → 정말 필요하면 다시 만들면 됨.

### 2.5 차세대 phase 선언 프롬프트는 단일 코드 펜스로
오케스트레이터 인스턴스가 다음 phase를 작업 인스턴스에 던질 때, **출력 전체를 단일 코드 펜스(``` … ```)로 감싼다**. 이유: 채팅 UI 한 번 클릭 복사 버튼으로 통째 복사 → 작업 인스턴스에 그대로 붙여넣기. 펜스 밖 안내문은 사용자가 일일이 드래그해야 하므로 금지.

펜스 골격:
````
{Project} Phase {N} — {제목}.

상세 to-do: tasks/{Project}/{NN}_Phase{N}_*.md
선결 완료: Phase {M} — ...
직전 phase 인프라 컨텍스트 (이미 있는 것 + 본 phase가 확장할 것)

================================================================
[작업 시작 전 사용자 확정 필요 사항 N건]
================================================================
1. 질문 / default 추천 (변경 가능)
2. ...
답변 받기 전엔 코드 변경 시작 X.

================================================================
[Step 1] {제목}
================================================================
구체 지시 (파일/시그니처/호출처 명시)

[Step 2] ... [Step N] ...

================================================================
꼬임 방지 원칙
================================================================
1. {phase 특이 함정}
2. {기존 인프라 보존 사항}
3. 사용자 결정 N건 받고 시작
...

================================================================
완료 후 회고
================================================================
{phase to-do md} 하단에 회고 추가 후 done/ 이동.
회고 항목: {결정 답변 / 검증 결과 / 재사용 통계 / 영향 범위}
다음 phase: {다음 to-do md 경로}
````

규칙:
- 출력 전체 단일 코드 펜스로. 펜스 밖 안내문 금지.
- 펜스 안 코드 블록 필요하면 4-tick(```` ``` ````) 또는 `~~~`로 외곽 펜스 차별화.
- 결정 필요 사항은 항상 default 추천 + "받기 전엔 코드 변경 X" 1줄 못 박음.
- Step은 파일명 / 클래스 / 시그니처 / 호출처 구체로 → 추상 지시는 작업 인스턴스가 다시 탐사해야 함 (시간 낭비).
- 꼬임 방지 원칙은 직전 phase lessons / 메모리 룰 중 본 phase에서 건드리는 영역만 추출.
- 완료 후 회고 항목 + 다음 phase 경로까지 명시 → 마무리 후 무엇을 할지 모호함 0.

---

## 3. 코드 / 설계 원칙

### 3.1 YAGNI
- 단일 최소 코드 우선. 설정 노출은 정말 필요할 때만.
- 동적 계산 vs 상수: 입력 검증 좁은 동적 계산은 silent failure 위험. "사용자가 한 번 튜닝하면 되는 값"이면 상수.

### 3.2 모호한 입력 = 차단
- 자동 보정이 사용자 의도와 일치 확신 못 하면 throw.
- 잘못된 결과의 사후 발견 비용이 크면 throw.
- 둘 다 NO면 차단. 사용자가 입력을 다시 정리하게.

### 3.3 Idempotency 가정
- "도구를 두 번 돌리면?" → 모든 자동화 도구 검증 기본.
- 1차 산출물 안정, 2차에 꼬임 함정 흔함.

### 3.4 단일 vs 멀티 케이스
- 단일 케이스만 검증한 phase는 멀티 케이스 검증 필수.
- 데이터 모델: "1:1인가 1:N인가" 명시적으로. 단일 필드 + "첫 항목 우선" = 멀티 무성 실패.

### 3.5 정규화는 비교 양쪽 모두에
- Prefix strip / case fold / trim 같은 정규화 헬퍼는 비교 호출 지점 모두 일관 적용.
- 5곳 넘으면 wrapper API로 강제.

### 3.6 미러링 함수는 동기화 명시
- 미리보기/시뮬레이션이 실제 변환 로직 미러링하면 "원본 변경 시 동기화 필요" 주석.
- 가능하면 단일 출처 + read-only 모드 옵션.

### 3.7 파괴적 자동화 = 명시 리스트 + 휴지통 + 사후 검증
- 광범위 스캔+패턴 삭제 금지. 호출자가 명시 path 전달.
- 플랫폼이 제공하는 휴지통/restore 경로로 복구 가능하게.

### 3.8 안전 분기엔 opt-out 함께
- 격리/경고 분기엔 [Skip] / [Convert Anyway] 우회 동선 디자인. 사용자 의도 우선.

### 3.9 에러 처리는 경계에서만
- 내부 코드/프레임워크 보장은 신뢰. 사용자 입력·외부 API·파일 I/O에서만 방어.
- 일어날 수 없는 시나리오의 fallback/validation 금지.

### 3.10 추상화 보류
- 비슷한 세 줄은 조기 추상화보다 낫다. 가설적 미래 요구사항 위해 설계 X.

---

## 4. 플랫폼/프레임워크 함정 (프로젝트별 적층)

본 섹션은 **프로젝트별로 채워나가는 영역**. 도메인 정해진 직후, 해당 플랫폼의 "처음 만지면 반드시 꼬이는" 함정 5~10개 적층.

적층 트리거:
- 같은 함정 두 번 빠지면 lessons.md → 본 문서 해당 섹션 보강.
- 외부 SDK 업데이트로 동작 바뀌면 즉시 갱신.

체크리스트 예시 (도메인별로 다름):
- 빌드/배치 컨텍스트에서 금지된 API 호출
- 핫리로드/캐시 무효화 타이밍
- 비동기 ↔ 동기 경계 데드락 패턴
- 직렬화 포맷 변경 시 마이그레이션 함정
- 플랫폼별 경로 구분자 / 인코딩 함정
- GPU/렌더링/네트워킹 비결정성
- 패키지 매니저 의존성 해결 순서

> 비어 있으면 비워둠. 가짜 채우기 금지.

---

## 5. 매칭 / 네이밍 컨벤션 (해당 시)

파일·심볼·식별자 매칭이 핵심 로직인 프로젝트에서만 적용.

- 매칭 알고리즘은 다단 폴백: **정확 일치 → 정규화 일치 → alias 매칭 → 부분 토큰 매칭**.
- 각 단계 통과 항목은 출처 단계 메타데이터로 기록 (디버깅용).
- 사용자 명시 alias는 자동 후보보다 우선.
- 매칭 실패 항목은 silent drop 금지 → unmatched 리스트로 노출, 수동 매핑 UI 제공.
- Generic/디폴트 이름(`1`, `2`, `Item.001`)은 자동 매칭 본질 불가 → 자동 분기에서 빼고 수동으로.
- 외부 컨벤션 변환 시 일부 토큰만 처리 (예: prefix 그대로, suffix만 변환).

---

## 6. UI / UX 설계 (해당 시)

### 6.1 사전 가시화 우선
- Pending/Quarantine/Resolve 워크플로우는 인터랙티브 환경에선 redundant.
- 변환 *전* 다이얼로그(Unknown / Unmatched / Loose) + 트리 영역이 본질.

### 6.2 파괴 작업 롤백은 명시 버튼만, 전역 Undo 차단
- 프레임워크 Undo 콜백 hook 위험. 무관한 Undo가 일괄 롤백 트리거되면 사고.
- auto-cleanup이 휴지통 이동이면 Undo 자체 불필요.

### 6.3 한국어 UI / 영어 코드 (단일 팀 한국어 환경 기준)
- Tooltip / Header / HelpBox / Dialog / 사용자 보는 로그 → 한국어 inline.
- 주석 / Debug.Log / 예외 메시지 → 영어.
- **i18n Localization 레이어는 단일 팀 내부 도구에서 YAGNI**. 외부 배포 시점에 도입.

### 6.4 일괄 토글 + 개별 opt-out
- 그룹 단위 일괄 3-state 토글이 첫 화면. 개별 토글은 opt-out으로 보존.

---

## 7. 정리 / 패키징

- **동작 변화 0의 안전 리팩토링**: partial 분할 / 폴더 rename / 통합. 신규 기능 0. 회귀 시나리오 통과.
- 재생성될 transient 폴더는 cleanup 만들지 X.
- 필요 없는 거대 레이어는 폴더 통째 삭제. 부분 잔존 금지.
- 네임스페이스 마지막 segment를 흔한 타입명과 겹치지 않게.
- 패키지화 시: `package.json` + `Documentation~/` (또는 동등물) + 빈 프로젝트 복사 검증.
- 경로 하드코딩 검사 (모든 경로는 Settings/Config에서만).
- 배포 전 체크리스트는 별도 md로 (포맷 일관성, 빌드 산출물 검증, 문서 링크 깨짐).

---

## 8. 메모리 / Lessons 운용

- 사용자 교정 → 프로젝트별 `tasks/lessons.md`에 즉시 추가. 형식: **증상 / 원인 / 규칙 / 적용 시점** (+ 선택: **검증 함정**).
- 전역 메모리(`memory/`)에는 향후 phase에서 일관 적용할 룰만. 같은 주제 중복 금지, 기존 update 우선.
- 외부 노출 사실(파일 경로 / 함수명)은 **사용 직전 검증**. 메모리는 작성 시점 기준 사실.
- 세션 시작 시 해당 프로젝트 lessons.md 1회 읽기.

---

## 9. 커뮤니케이션 (Claude 행동 규약)

- **모호한 한국어 피드백은 첫 액션 전 1줄 확인**. 대조 어휘("빼다", "올리다", "넣다")는 다의어 → "(a) ... (b) ... 어느 쪽?". 시각 자료(스크린샷) 동반되면 그것이 disambiguation 1순위.
- 메모리 기반 우선 해석은 1차 시그널일 뿐, 유일 해석 X → "다른 합리적 해석은?" 1회 자문.
- 비전 정책 충돌 시 옵션 제시(always-on / 토글 / 정지) 후 사용자 선택. 합의 변경은 phase 문서에 "(사용자 합의 YYYY-MM-DD)" 표기.
- Commit message에 Claude co-author trailer X (사용자 별도 지시 시에만).
- 외부 원인 입력 부재로 인한 변환 실패는 콘솔 에러로 둠. Pending 격상 금지.
- "안전 측면에서 한 번 더" 같은 직관적 안전망 추가 전 자문 → 사전 가시화로 충분하면 redundant.

---

## 10. Codex (외부 LLM) 협업 사이클 (해당 시)

비주류 설계/구현 작업에 외부 LLM을 보조로 쓰는 경우. Claude는 직접 호출 X, 적절한 시점에 슬래시 명령 **한 번** 추천.

표준 사이클:
1. **설계 — Claude**: plan mode에서 설계 md 작성.
2. **적대 리뷰 — 외부 LLM**: Claude가 "Recommend `/codex-adversarial <md>`" 한 줄 추천 → 사용자 명시 호출 → 결과 자동 저장.
3. **설계 정제 — Claude**: 저장된 md 읽고 각 피드백 평가(accept / reject / defer) → 사용자 확인 후 설계 갱신.
4. **구현 — Claude**: 정제된 설계 기반 구현.
5. **코드 리뷰 — 외부 LLM**: "Recommend `/codex-review <path>`" 추천 → 사용자 호출 → 결과 자동 저장.
6. **피드백 적용 — Claude**: 분류 보고 → 사용자 확인 후 accepted 적용.

적용 기준:
- **적용**: 3+ 파일 수정, 새 아키텍처, 어려운 버그, 성능 critical, 장기 유지 코어 모듈.
- **스킵**: 단일 함수 튜닝, 오타/린트, 단순 rename. 추천도 X (노이즈 회피).

원칙:
- 외부 LLM은 보조. 최종 판단은 Claude + 사용자.
- 추천은 한 작업당 한 번. 같은 단계 반복 추천 X.
- 소규모 작업은 1–3 스킵, 4 → 5 → 6만.

---

> 신규 프로젝트 시작 시: 본 문서 + 해당 프로젝트 Vision 문서 → Phase 0 지시.
> 새 도메인(언어/프레임워크/SDK) 작업으로 본 문서 적용 한계 만나면, 해당 프로젝트 lessons.md에 누적 → 안정 후 본 문서 해당 섹션(특히 §4) 보강.
