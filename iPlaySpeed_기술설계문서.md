# iPlaySpeed 기술 설계 문서

> 멀티 게임 일일퀘스트 런처 · v0.1 설계안
> 작성일: 2026-06-09

---

## 1. 한 줄 요약

여러 게임의 일일 숙제를 alt+tab 없이 한 화면에서 추적하고, 다음에 켤 게임을 버튼 한 번으로 실행해주는 **항상 위에 떠 있는 오버레이형 멀티 게임 런처**.

---

## 2. 추천 기술 스택과 그 이유

결론부터: **C# / .NET 8 + WPF** 로 만드는 것을 추천합니다.

이 프로그램이 요구하는 기능 대부분이 "Windows 운영체제 깊숙한 곳"을 건드리는 일입니다. 게임 위에 오버레이를 띄우고, 전역 단축키를 잡고, 앱별 볼륨을 조절하고, 미디어 재생을 제어하고, 프로세스를 실행/종료하는 일은 모두 Windows 네이티브 API를 직접 호출해야 합니다. C#/.NET은 이 API들을 가장 직접적이고 안정적으로 다룰 수 있는 환경입니다.

각 후보를 비교하면:

| 항목 | C#/.NET WPF (추천) | Electron | Python (PyQt) |
|---|---|---|---|
| 항상 위 오버레이 | 기본 지원 (`Topmost`) | 가능하나 게임 위 표시가 불안정 | 가능하나 손이 많이 감 |
| 앱별 볼륨 제어 | NAudio로 깔끔 | 네이티브 모듈 추가 필요 | pycaw로 가능 |
| 미디어 컨트롤(SMTC) | 공식 API 바로 호출 | 래퍼 필요 | 래퍼 필요 |
| 전역 단축키 | Win32 API 직접 | 라이브러리 의존 | 라이브러리 의존 |
| 프로세스 실행/종료 | 1급 지원 | 가능 | 가능 |
| 단일 .exe 배포 | 매우 쉬움 | 무거움(100MB+) | 까다로움 |
| 메모리/성능 | 가볍고 빠름 | 무거움 | 보통 |

**비개발자 입문자에게 특히 좋은 점**: Visual Studio Community(무료)를 설치하면 화면을 마우스로 끌어다 배치하는 디자이너, 한 줄씩 멈춰가며 확인하는 디버거, 자동완성이 모두 들어 있어서 처음 배우기에 가장 친절합니다. 또한 게임 런처/오버레이 같은 Windows 전용 도구는 C# 예제와 커뮤니티 답변이 압도적으로 많습니다.

> 단, 이 분야는 입문 난이도가 낮지 않습니다. 그래서 뒤(8장)에 "기능별 난이도"를 표시해두었으니, 쉬운 것부터 하나씩 완성하는 방식을 권합니다.

---

## 3. 시스템 아키텍처 (큰 그림)

프로그램을 레고처럼 7개 모듈로 나눕니다. 각 모듈은 한 가지 일만 잘하면 됩니다.

```
┌─────────────────────────────────────────────────────┐
│                   iPlaySpeed 본체                      │
│                                                       │
│  ┌──────────────┐   ┌──────────────────────────┐     │
│  │ 게임 등록/관리 │   │   오버레이 UI (항상 위)    │     │
│  │  (Library)   │──▶│  게이지바·체크포인트·버튼   │     │
│  └──────┬───────┘   └────────┬─────────────────┘     │
│         │                    │                        │
│  ┌──────▼───────┐   ┌────────▼────────┐  ┌─────────┐  │
│  │ 진행도/리셋    │   │  게임 실행기      │  │ 단축키   │  │
│  │ (Progress)   │   │  (Launcher)     │  │(Hotkey) │  │
│  └──────────────┘   └─────────────────┘  └─────────┘  │
│                                                       │
│  ┌──────────────┐   ┌─────────────────┐               │
│  │ 미디어/볼륨    │   │ 업데이트 감지     │               │
│  │ (MediaCtrl)  │   │ (UpdateScan)    │               │
│  └──────────────┘   └─────────────────┘               │
│                                                       │
│  ┌─────────────────────────────────────────────┐     │
│  │ 데이터 저장 (games.json · history.json)        │     │
│  └─────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────┘
```

- **게임 등록/관리(Library)**: 등록된 게임 목록, 이름, 아이콘, 실행 경로를 보관.
- **오버레이 UI**: 화면 맨 위에 작게 떠 있는 게이지바 + 체크포인트 + 다음 버튼 + 미디어 컨트롤.
- **진행도/리셋(Progress)**: 오늘 몇 개 게임을 끝냈는지 계산, 설정 시각이 되면 자동 초기화.
- **게임 실행기(Launcher)**: 현재 게임 종료 → 다음 게임 실행, 런처 자동 클릭.
- **단축키(Hotkey)**: 오버레이 켜기/끄기, 다음 게임 등 전역 키 입력.
- **미디어/볼륨(MediaCtrl)**: 음악·영상 재생 컨트롤, 게임/미디어 볼륨 따로 조절.
- **업데이트 감지(UpdateScan)**: 게임 파일 수정 날짜를 읽어 정렬 순서 결정.

---

## 4. 데이터 저장 구조

복잡한 데이터베이스 없이, 사람이 읽을 수 있는 **JSON 파일 2개**로 시작합니다. 나중에 데이터가 많아지면 SQLite로 갈아탈 수 있습니다.

`games.json` — 등록된 게임 정보:

```json
[
  {
    "id": "zzz",
    "name": "젠레스 존 제로",
    "iconPath": "icons/zzz.png",
    "exePath": "D:/Games/ZenlessZoneZero/launcher.exe",
    "launcherType": "hoyoplay",
    "gameFolder": "D:/Games/ZenlessZoneZero",
    "order": 1,
    "estimatedMinutes": 8
  }
]
```

`history.json` — 일일 기록(리더보드용):

```json
[
  { "date": "2026-06-09", "completedGames": 7, "totalMinutes": 73, "startTime": "06:30", "endTime": "07:43" }
]
```

설정값(리셋 시각, 단축키, 오버레이 위치 등)은 `settings.json` 에 따로 저장합니다.

---

## 5. 기능별 구현 설계

각 기능을 "무엇을 / 어떻게 / 무슨 기술로 / 주의점" 순서로 정리합니다.

### 5-1. 간편한 게임 등록 (이름·아이콘 자동 수집)

- **어떻게**: 사용자가 게임 실행 파일(.exe)을 드래그하거나 선택하면, 그 파일에서 정보를 자동으로 뽑아냅니다.
- **기술**:
  - 아이콘 추출 → `System.Drawing.Icon.ExtractAssociatedIcon(exePath)` 로 exe에 박힌 아이콘을 그대로 가져옵니다.
  - 게임 이름 추출 → exe의 파일 속성(`FileVersionInfo.ProductName`)을 1차로 쓰고, 비어 있으면 폴더명/파일명을 후보로 제시 후 사용자가 확정.
- **주의점**: 일부 게임은 "런처 exe"와 "실제 게임 exe"가 다릅니다(예: HoYoPlay 계열). 등록 시 둘 다 잡아두면 좋습니다(런처 = 실행용, 게임 = 업데이트 감지·종료 감지용).
- **난이도**: ★★☆☆☆

### 5-2. 오버레이 게이지바 (항상 위 + 드래그 이동)

- **어떻게**: 테두리 없는 작은 창을 만들어 화면 맨 위에 띄웁니다. 게임이 전체화면이어도 위에 보이도록 합니다.
- **기술**:
  - WPF 창 속성: `Topmost = true`, `WindowStyle = None`, `AllowsTransparency = true`, `ShowInTaskbar = false`.
  - 드래그 이동: 창 위에서 마우스를 누르고 끌면 `DragMove()` 호출.
  - 게이지바: 등록 게임 수만큼 칸을 나누고, 완료한 게임 비율만큼 채움. 칸 사이마다 게임 아이콘을 체크포인트로 배치.
- **주의점**: **전체화면 독점(exclusive fullscreen)** 게임 위에는 어떤 오버레이도 뜨지 않습니다. 해결책은 게임을 "테두리 없는 창모드(borderless windowed)"로 두도록 안내하는 것입니다. 대부분의 일일퀘스트 게임은 이 모드를 지원합니다.
- **난이도**: ★★★☆☆

### 5-3. 전체 진행도 및 체크포인트  *(개정됨)*

- **확정 규칙**: "잠깐 켰다 꺼도 완료"가 아니라, **실제 게임 창을 최소 N분 이상 플레이해야 완료**로 처리합니다. (기본값 N = 5분, 게임마다 따로 설정 가능)

- **업데이트 오판정 문제 (RinTaro 지적)**: 게임을 켜서 업데이트만 받고 재실행하는 경우, "껐다 켰을 뿐인데" 완료로 잡히면 안 됩니다. 게다가 일일퀘스트는 5~10분, 업데이트는 10분 넘게 걸리기도 해서 단순히 "N분 이상 켜져 있었다"로도 구분이 안 됩니다.

- **해결 설계 — "게임 창 포그라운드 누적 시간"으로 판정**:
  - 핵심 아이디어: 켜져 있던 시간이 아니라 **실제 게임 창이 화면 맨 앞(foreground)에 떠 있던 시간**만 누적합니다.
  - 업데이트 중에는 화면 앞에 있는 것이 **런처/업데이터 창**이지 게임 창이 아닙니다. 그래서 10분짜리 업데이트를 받아도 "게임 창 포그라운드 시간"은 거의 0분 → 완료로 잡히지 않습니다.
  - 등록 시 **런처 exe와 실제 게임 exe를 구분**해 두고, 누적 시간은 오직 "게임 exe의 창"이 foreground일 때만 셉니다.
  - 누적 시간은 **같은 일일 런 안에서는 재실행을 넘어 유지**됩니다. 즉 업데이트 → 재시작 → 실제 플레이로 이어져도, 업데이트 후의 진짜 플레이 시간이 N분을 채우면 그때 완료 처리됩니다.
  - **이중 안전장치 (업데이트 감지)**: 이 세션 동안 게임 폴더 용량/파일이 크게 바뀌었는데(=업데이트 발생) 게임 포그라운드 시간이 N분 미만이면, "업데이트만 한 것으로 보입니다 — 일일퀘스트는 아직 미완료"라고 명확히 표시합니다.

- **판정 로직 요약**:
  ```
  매 1초:
    창이_게임exe의_것이고_foreground이면 → 그 게임의 활성플레이초 += 1
  완료 = (활성플레이초 >= N*60)
  단, 이번 세션에 게임폴더가 크게 변경됨(업데이트) AND 활성플레이초 < N*60
       → 상태 = "업데이트만 함 / 미완료"
  ```

- **기술**: foreground 창 추적은 Win32 `GetForegroundWindow` + `GetWindowThreadProcessId` 로 1초 타이머마다 확인. 프로세스 종료는 `Process.Exited`. 이 판정 로직(시간 누적·업데이트 보정·일일 경계)은 **Windows에 의존하지 않는 순수 계산**이라 별도 엔진으로 분리해 자동 테스트로 검증했습니다.
- **수동 보정**: 오판정 대비, 오버레이에서 특정 게임을 **수동으로 완료/미완료 토글**하는 버튼도 둡니다.
- **난이도**: ★★★☆☆

### 5-4. 다음 게임 빠른 실행 (현재 종료 → 다음 실행)

- **어떻게**: 오버레이의 "다음" 버튼/단축키를 누르면 현재 게임을 닫고 곧바로 다음 순번 게임을 실행.
- **기술**:
  - 종료: `process.CloseMainWindow()` (정상 종료 시도) → 안 되면 `process.Kill()`.
  - 실행: `Process.Start(exePath)`.
- **주의점**: 게임을 강제 종료(Kill)하면 저장이 안 될 수 있습니다. 가급적 정상 종료를 먼저 시도하고, 일정 시간 기다린 뒤에만 강제 종료하도록 설계합니다.
- **난이도**: ★★☆☆☆

### 5-5. 업데이트 주기 기반 정렬  *(개정됨 — 자동 학습 방식)*

- **기획 의도**: 신규 업데이트가 임박한 게임일수록 못 깬 이벤트가 쌓여 있어 오래 붙잡게 되므로, 흐름이 끊기지 않도록 **맨 마지막에** 플레이하도록 정렬.

- **RinTaro 지적**: 게임마다 패치 주기가 비슷하지만 완벽히 일정하진 않다. 그렇다고 매번 수동 입력하면 이 프로그램을 쓸 이유가 줄어든다. → **최대한 자동으로** 공개/관측 정보를 받아와야 한다.

- **해결 설계 — "관측 기반 자가 학습" (수동 입력 없음)**:
  - 프로그램이 평소에 각 게임 폴더의 변경을 조용히 관측합니다. 폴더 용량/파일 수가 **크게 늘어나는 순간 = 대형 패치**로 보고, 그날을 **패치 발생일로 자동 기록**합니다.
  - 이 기록이 쌓이면 게임별 **실제 패치 간격(중앙값)** 을 스스로 계산합니다. 예: 어떤 게임이 41일·43일·42일 간격으로 패치됐다 → 학습된 주기 ≈ 42일.
  - **다음 패치 예정일 = 마지막 패치일 + 학습된 주기**. "오늘부터 다음 패치까지 남은 일수"를 게임마다 구합니다.
  - **정렬**: 남은 일수가 **많은**(=막 패치돼서 한가한) 게임을 **앞**으로, 남은 일수가 **적은**(=패치 임박, 백로그 많은) 게임을 **뒤**로. → 기획 의도와 정확히 일치.
  - **수동 입력이 필요 없습니다.** 프로그램을 쓰는 동안 알아서 각 게임의 패치 리듬을 배웁니다. 쓸수록 정확해집니다.

- **데이터가 아직 부족할 때(콜드 스타트)**:
  - 관측된 패치가 2회 미만이면 아직 주기를 모릅니다. 이때는 ① 폴더 최종 수정일을 임시 패치일 추정치로 쓰고, ② 전역 기본 주기(설정값, 기본 42일)를 임시로 적용합니다.
  - 실제 패치를 2회 이상 관측하면 그때부터는 **학습값이 기본값을 덮어씁니다.** 기본값은 처음 몇 주만 쓰이는 임시 버팀목입니다.

- **(선택) 온라인 보강**: 더 정확히 하고 싶으면, 향후 공개 패치 캘린더(가챠 일정 사이트 등)를 켜고 끌 수 있는 **선택 기능**으로 붙일 수 있습니다. 단 사이트 구조가 바뀌면 깨질 수 있어 **보조 신호**로만 쓰고, 기본 동작은 위의 자가 학습입니다.

- **기술**: 폴더 스캔은 `DirectoryInfo` 로 총 용량·파일 수·최종 수정일 스냅샷을 주기적으로 저장. 큰 증가분 감지 시 패치 이벤트 기록. 주기 계산·다음 패치 예측·정렬은 **순수 계산 로직**이라 별도 엔진으로 분리해 자동 테스트로 검증했습니다.
- **난이도**: ★★★★☆

### 5-6. 런처 로그인/플레이 버튼 자동화

- **어떻게**: 전용 런처를 거치는 게임(HoYoPlay 등)에서, 런처가 뜨면 "플레이" 버튼을 자동으로 눌러 게임까지 자동 진입.
- **기술 후보**:
  - UI 자동화: `FlaUI` / Windows UI Automation 으로 버튼을 찾아 클릭.
  - 좌표 클릭: 버튼 위치를 좌표로 눌러줌(런처 UI가 바뀌면 깨지기 쉬움).
  - 명령줄 실행: 일부 게임은 런처를 건너뛰고 게임 exe를 인자와 함께 직접 실행할 수 있음(가장 안정적, 게임마다 가능 여부 다름).
- **⚠️ 매우 중요한 주의점 (법적/계정 리스크)**:
  - 런처를 자동 클릭하거나 우회하는 행위는 **게임사 이용약관(ToS) 위반**이 될 수 있고, 안티치트가 비정상 동작으로 탐지할 위험이 있습니다. 최악의 경우 **계정 정지**로 이어질 수 있습니다.
  - 자동 로그인(아이디/비밀번호 입력 자동화)은 **하지 않습니다**. 보안 위험이 크고 약관 위반 소지가 높습니다.

- **확정 설계 (RinTaro 합의)** — 안전 우선의 선택형 자동 클릭:
  - **기본값은 꺼짐.** 자동 클릭은 사용자가 위험을 이해하고 직접 켜야만 동작하는 **선택 옵션**입니다.
  - 켤 때 **경고 문구 다이얼로그**를 띄워, 약관/계정 정지 위험을 명시하고 "이해했고 내 책임으로 사용한다"에 체크해야만 활성화됩니다.
  - 클릭 방식은 매크로식 가짜 입력이 아니라, **OS 수준의 실제 마우스 클릭 입력**(Win32 `SendInput`)을 게임이 알려준 "플레이" 버튼 위치에 보냅니다. → 운영체제 입장에서 **사람이 실제로 마우스로 클릭한 것과 동일한 판정**이 나도록 최대한 안전하게 처리.
  - 버튼 위치는 ① UI Automation으로 자동 탐색을 시도하고, 실패하면 ② 사용자가 한 번 "여기가 플레이 버튼"이라고 찍어 보정(calibration)하는 방식.
  - 비밀번호/계정 정보는 **저장하지도, 입력하지도 않습니다.**
- **난이도**: ★★★★★ (기술·법적 양면에서 가장 까다로움 → 로드맵 마지막 단계)

### 5-7. 미디어 컨트롤 + 볼륨 분리

- **어떻게**: 오버레이 하단/옆에 재생 중인 음악·영상의 이전/일시정지/다음 버튼과, 게임 볼륨·미디어 볼륨을 따로 조절하는 슬라이더를 둡니다.
- **기술**:
  - 미디어 제어 → Windows **SMTC**(`Windows.Media.Control` 네임스페이스). YouTube(브라우저)·Spotify 등 SMTC에 연동된 앱을 이전/정지/다음으로 제어 가능. 직접 다루기 번거로우면 오픈소스 래퍼 `WindowsMediaController` 활용.
  - 앱별 볼륨 → **NAudio**의 CoreAudio(`ISimpleAudioVolume`/오디오 세션) 로 "게임 프로세스"와 "음악 앱 프로세스"의 볼륨을 각각 조절.
- **주의점**: SMTC는 앱이 SMTC에 연동되어 있어야 제어됩니다(대부분의 주요 음악/영상 앱은 연동됨). 또한 이 API는 백그라운드 서비스/SYSTEM 권한으로 실행하면 동작하지 않으니, 일반 사용자 프로그램으로 실행해야 합니다.
- **난이도**: ★★★★☆

### 5-8. 일일 소요 시간 기록 + 리더보드

- **어떻게**: 그날 첫 게임을 시작한 시각부터 마지막 게임(100% 달성)까지 걸린 시간을 측정해 `history.json`에 저장하고, 날짜별 기록을 표/그래프로 보여줍니다.
- **기술**: `Stopwatch`/`DateTime` 으로 시간 측정, WPF 차트(LiveCharts 등)로 추이 표시.
- **난이도**: ★★☆☆☆

### 5-9. 일일퀘스트 리셋 시각 자동 초기화

- **어떻게**: 사용자가 "내 일일 런 시작 기준 시각"(예: 06:30)을 설정. 프로그램이 켜져 있는 동안 그 시각을 지나면 진행도를 0%로 자동 초기화.
- **기술**: 1분마다 현재 시각을 확인하는 타이머(`DispatcherTimer`). 마지막 리셋 날짜를 저장해 두고, 설정 시각을 넘긴 새 날이면 초기화.
- **주의점**: 게임마다 실제 리셋 시각(서버 기준)이 다릅니다. 기획대로 "개인이 정한 하나의 런 시작 시각"을 기준으로 통합 관리하는 방식이 단순하고 좋습니다. (게임별 개별 리셋 시각까지 반영하는 건 2단계 고도화)
- **난이도**: ★★☆☆☆

---

## 6. 사용할 핵심 라이브러리 정리

| 용도 | 라이브러리 / API | 비고 |
|---|---|---|
| UI 프레임워크 | WPF (.NET 8) | Windows 내장 |
| 앱별 볼륨 | NAudio (CoreAudio) | NuGet 무료 |
| 미디어 컨트롤 | Windows.Media.Control (SMTC) + WindowsMediaController | 공식 API + 오픈소스 래퍼 |
| 전역 단축키 | Win32 `RegisterHotKey` | 직접 호출 |
| 아이콘 추출 | System.Drawing | 내장 |
| 런처 UI 자동화(선택) | FlaUI / UI Automation | 리스크 있음 |
| 차트 | LiveCharts2 | 리더보드 그래프 |
| JSON 저장 | System.Text.Json | 내장 |

---

## 7. MVP 범위 (1차 목표) vs 후순위

처음부터 9개 기능을 다 만들면 지칩니다. **"오버레이로 진행도 보고 다음 게임 켜기"** 가 이 프로그램의 심장이므로, 그것부터 완성합니다.

**MVP (먼저 만들 것)**
1. 게임 등록(이름·아이콘 자동) + 목록 저장 (5-1)
2. 항상 위 오버레이 게이지바 + 체크포인트 + 드래그 이동 (5-2)
3. 게임 실행 + 종료 감지로 진행도 채우기 (5-3)
4. "다음 게임" 버튼으로 현재 종료 → 다음 실행 (5-4)
5. 전역 단축키로 오버레이 켜기/끄기 (5-2의 일부)
6. 설정 시각 기준 진행도 자동 리셋 (5-9)

**2단계 (그 다음)**
7. 미디어 컨트롤 + 게임/미디어 볼륨 분리 (5-7)
8. 일일 소요 시간 기록 + 리더보드 (5-8)
9. 업데이트 날짜 기반 정렬 (5-5)

**3단계 / 신중하게 (리스크 검토 후)**
10. 런처 자동화 (5-6) — 법적·계정 리스크 때문에 가장 마지막에, 선택 옵션으로.

---

## 8. 개발 로드맵 (단계별)

| 단계 | 목표 | 결과물 |
|---|---|---|
| 0 | 개발환경 준비 | Visual Studio + 빈 WPF 프로젝트 실행 확인 |
| 1 | 게임 등록 화면 | exe 선택 → 이름·아이콘 자동 → 목록에 추가/저장 |
| 2 | 오버레이 띄우기 | 화면 위 작은 창 + 드래그 이동 + 단축키 토글 |
| 3 | 게이지바 연결 | 등록 게임 수만큼 칸 생성 + 아이콘 체크포인트 |
| 4 | 실행/종료 감지 | 게임 켜고 끄면 게이지 1칸 차오름 |
| 5 | 다음 게임 실행 | "다음" 버튼으로 종료 후 다음 게임 실행 |
| 6 | 자동 리셋 | 설정 시각 지나면 0%로 초기화 |
| --- | **여기까지 = MVP 완성** | 실사용 가능한 첫 버전 |
| 7 | 볼륨/미디어 | 오버레이에 재생 컨트롤·볼륨 슬라이더 |
| 8 | 리더보드 | 일일 소요 시간 기록·그래프 |
| 9 | 업데이트 정렬 | 파일 날짜로 게임 순서 자동 정렬 |
| 10 | 런처 자동화(선택) | 리스크 안내 후 옵션으로 제공 |

---

## 9. 미리 알아둘 기술적·법적 리스크

1. **전체화면 게임 위 오버레이**: 독점 전체화면에는 오버레이가 안 뜹니다. → 게임을 "테두리 없는 창모드"로 두도록 안내가 필요합니다.
2. **런처 자동화 = 약관/계정 리스크**: 가장 주의해야 할 부분. 자동 로그인은 권장하지 않으며, 자동 클릭도 사용자가 위험을 이해하고 켜는 선택 옵션으로만. 안티치트 탐지 가능성 존재.
3. **강제 종료 시 데이터 손실**: 게임을 Kill하면 진행 저장이 안 될 수 있음 → 정상 종료 우선.
4. **파일 날짜 ≠ 정확한 패치 주기**: 업데이트 정렬은 보조 정보(공식 주기)와 병행하는 것이 정확.
5. **관리자 권한**: 다른 게임 프로세스를 종료하려면 iPlaySpeed가 게임과 같거나 높은 권한으로 실행돼야 할 수 있음.

---

## 10. 비개발자 입문자를 위한 다음 단계

지금 바로 할 수 있는 첫걸음:

1. **Visual Studio Community(무료)** 설치 → 설치 시 ".NET 데스크톱 개발" 항목 체크.
2. 빈 **WPF 앱(.NET 8)** 프로젝트를 만들어 "실행"이 되는지 확인 (빈 창이 뜨면 성공).
3. 위 로드맵의 **1단계(게임 등록 화면)** 부터 시작.

원하시면 제가 다음 중 무엇이든 이어서 만들어 드릴 수 있습니다:
- 1단계 "게임 등록 화면"의 실제 동작하는 시작 코드
- 각 기능별로 더 자세한 구현 가이드(코드 포함)
- 오버레이 UI 화면 디자인 시안

---

*이 문서는 v0.1 설계안입니다. 기능 규칙(완료 판정 기준, 정렬 가중치, 단축키 등)은 만들면서 함께 조정하면 됩니다.*
